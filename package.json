{
  "private": true,
  "workspaces": [
    "packages/core"
  ],
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "conventional-changelog-eslint": "^3.0.1",
    "lerna": "^3.13.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.4.1"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/__template__/"
    ]
  }
}
